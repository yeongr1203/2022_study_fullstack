<?php
    $n1 = 10;
    $n2 = 20;
    $sum = $n1 + $n2; // = 30 (10+20) 
    //$sum은 변수만 올수있음. $sum = 이거 다음자리에는 변수와 리터럴이 들어올 수 있다.

    print $sum;

    print "<br>"; // 줄 줘서 띄어써주기

    $sub = 12.3 - 42.72; //  = -30.42 (= 12.3 - 42.72 )
    print $sub;
    //리터럴(literal) : 원래 본인의 값, 변하지 않는 값을 리터럴이라고 함. (상수는 비슷하지만 한번 대입된 값이 안바뀌는 게 상수, 대입되기 전엔 언제든 변할수있다.)
    
?>